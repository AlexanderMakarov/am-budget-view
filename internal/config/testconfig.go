package config

// TestConfigPath is the stable config fixture for automated tests.
// Do not use the repo-root config.yaml in tests — it may contain local user settings.
const TestConfigPath = "testdata/config.yaml"
